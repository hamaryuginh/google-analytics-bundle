<!-- Google Analytics -->
<script>
    (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
                (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
            m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
    })(window,document,'script','//www.google-analytics.com/analytics{{ debug ? '_debug' }}.js','ga');
    {% if debug %}
//        window.ga_debug = {trace: true}; // too much verbose
    {% endif %}

    ga('create', '{{ account }}', 'auto');
//    ga('require', 'ec'); // To activate Enhanced Ecommerce plugin
//    ga('send', 'pageview'); // At the end of the webpage

    function createFunctionWithTimeout(callback, opt_timeout) {
        var called = false;
        function execCallback() {
            if (!called) { called = true; callback(); }
        }
        setTimeout(execCallback, opt_timeout || 1000);
        return execCallback;
    }

</script>
<!-- End Google Analytics -->
